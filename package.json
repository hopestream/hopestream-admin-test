{
  "name": "hopestream-admin",
  "private": true,
  "version": "1.1.0",
  "description": "The HopeStream administrative interface.",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "start": "http-server -a localhost -p 8000 -c-1 ./client"
  }
}
